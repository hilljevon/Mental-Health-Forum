'use client'
import { MongoUserProps } from '@/lib/types/types'
import React from 'react'

const UserContent = ({ userData }: { userData: MongoUserProps }) => {
    return (
        <>
            <h1>User Profile</h1>
        </>
    )
}

export default UserContent